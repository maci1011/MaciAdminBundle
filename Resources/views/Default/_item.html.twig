
<div class="row">

	<div class="col-xs-2">

		<input type="checkbox" name="select[]" value="{{ item.id }}" />

	</div>

	<div class="col-xs-1">

		<a href="{{ path('maci_admin_entity_form', { 'entity': admin.name, 'id': item.id }) }}">
			{{ item.id }}
		</a>

	</div>

	<div class="col-xs-9">

		<a href="{{ path('maci_admin_entity_form', { 'entity': admin.name, 'id': item.id }) }}">
			{{ item }}
		</a>

	</div>

</div>
